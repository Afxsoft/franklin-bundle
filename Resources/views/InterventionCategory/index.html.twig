{% extends 'FklFranklinBundle:InterventionCategory:common.html.twig' %}

{% set title = 'InterventionCategory list'|trans %}

{% block body -%}
<div class="panel panel-default">
    <table class="table table-striped table-hover table-responsive">
        <thead>
            <tr>
                <th>{% trans %}Id{% endtrans %}</th>
                <th>{% trans %}Name{% endtrans %}</th>
                <th><span class="glyphicon glyphicon-edit" title="{% trans %}Edit{% endtrans %}"></span></th>
                <th><span class="glyphicon glyphicon-remove" title="{% trans %}Delete{% endtrans %}"></span></th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
            <tr>
                <td><a href="{{ path('interventioncategory_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td><a href="{{ path('interventioncategory_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>
                    <a href="{{ path('interventioncategory_edit', {'id': entity.id}) }}" title="{% trans %}Edit{% endtrans %}">
                        <span class="glyphicon glyphicon-edit"></span>
                    </a>
                </td>
                <td>
                    <a href="{{ path('interventioncategory_delete', {'id': entity.id}) }}" title="{% trans %}Delete{% endtrans %}">
                        <span class="glyphicon glyphicon-remove"></span>
                    </a>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    {% if pages %}
    <div class="panel-footer">
        {% include 'DevelopatheCrudBundle::pagination.html.twig' with {
            currentFilters: { },
            currentPage: page,
            paginationPath: "interventioncategory",
            lastPage: pages,
            showAlwaysFirstAndLast: true
        } only %}
    </div>
    {% endif %}
</div>

{% endblock body %}

